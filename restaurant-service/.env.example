# ── Restaurant Service ────────────────────────────────────────────────────────
DATABASE_URL=postgres://postgres:postgres@localhost:5432/restaurantdb?sslmode=disable
REDIS_ADDR=localhost:6379
NATS_URL=nats://localhost:4222
GRPC_ADDR=:50052

# ── API Gateway ───────────────────────────────────────────────────────────────
USER_SERVICE_ADDR=localhost:50051
RESTAURANT_SERVICE_ADDR=localhost:50052
HTTP_ADDR=:8080

# ── Shared ────────────────────────────────────────────────────────────────────
# IMPORTANT: change this before deploying to production!
JWT_SECRET=change-me-in-production
